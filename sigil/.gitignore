node_modules/
storybook-static/
*.log

# Generated by `make -C catalyst dump-design` (real Twig output + enum colours).
# Regenerate rather than commit, so the Storybook fixtures never drift.
fixtures/
