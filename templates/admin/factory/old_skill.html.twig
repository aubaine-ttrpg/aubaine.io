{% extends 'base.html.twig' %}

{% block title %}Old Skill Factory{% endblock %}

{% block body %}
    <main class="max-w-6xl mx-auto px-4 py-8 space-y-6">
        <div class="flex items-center justify-between">
            <div>
                <p class="text-sm text-slate-400 space-x-1">
                    <a href="{{ path('admin_home') }}" class="hover:text-sky-300">Admin</a>
                    <span>/</span>
                    <a href="{{ path('admin_old_skill_index') }}" class="hover:text-sky-300">Old Skills</a>
                    <span>/</span>
                    <span class="text-slate-300">Create</span>
                </p>
                <h1 class="text-3xl font-semibold">Create Old Skill</h1>
            </div>
            <a class="btn btn-ghost" href="{{ path('admin_old_skill_index') }}">← Back to list</a>
        </div>

        {% include 'admin/old_skill/_form.html.twig' with { form: form, submit_label: 'Create old skill' } %}
    </main>
{% endblock %}
