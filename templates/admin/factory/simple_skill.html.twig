{% extends 'base.html.twig' %}

{% block title %}Simple Skill Factory{% endblock %}

{% block body %}
    <main class="max-w-5xl mx-auto px-4 py-8 space-y-6">
        <div class="flex items-center justify-between">
            <div>
                <p class="text-sm text-slate-400 space-x-1">
                    <a href="{{ path('admin_home') }}" class="hover:text-sky-300">Admin</a>
                    <span>/</span>
                    <a href="{{ path('admin_simple_skill_index') }}" class="hover:text-sky-300">Simple Skills</a>
                    <span>/</span>
                    <span class="text-slate-300">Create</span>
                </p>
                <h1 class="text-3xl font-semibold">Create Simple Skill</h1>
            </div>
            <a class="btn btn-ghost" href="{{ path('admin_simple_skill_index') }}">&larr; Back to list</a>
        </div>

        {% include 'admin/simple_skill/_form.html.twig' with {
            form: form,
            iconSrc: iconSrc ?? null,
            submit_label: 'Create simple skill'
        } %}
    </main>
{% endblock %}
