{% extends 'base.html.twig' %}

{% block title %}{{ 'book.settings.title'|trans }} · {{ book.title }}{% endblock %}

{% block body %}
    <div class="page-head">
        <h1>{{ 'book.settings.title'|trans }}</h1>
        <a class="btn btn--ghost" href="{{ path('app_book_edit', {id: book.id}) }}">{{ 'action.cancel'|trans }}</a>
    </div>

    <div class="panel panel--narrow">
        {{ form_start(form) }}
            {{ form_row(form.title) }}
            {{ form_row(form.subtitle) }}
            {{ form_row(form.bookType) }}
            {{ form_row(form.versionMajor) }}
            {{ form_row(form.versionMinor) }}
            <div class="form-actions">
                <button class="btn btn--primary" type="submit">{{ 'action.save'|trans }}</button>
            </div>
        {{ form_end(form) }}
    </div>
{% endblock %}
